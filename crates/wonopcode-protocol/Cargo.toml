[package]
name = "wonopcode-protocol"
version = "0.1.0"
edition = "2021"
description = "Shared protocol types for wonopcode client-server communication"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
